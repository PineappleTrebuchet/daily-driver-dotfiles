[opener]
runwin = [
	{ run = "wine %s", orphan = true, for = "unix" }
]
runsh = [
	{ run = "/bin/sh %s", desc = "Run", block = true, for = "unix" }
]


[open]
prepend_rules = [
	{ url = "*.exe", use = "runwin" },
	{ url = "*.sh", use = ["edit", "runsh"] },
]
